import { ClKeyFilterDirective } from './key-filter.directive';

describe('KeyFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ClKeyFilterDirective();
    expect(directive).toBeTruthy();
  });
});
