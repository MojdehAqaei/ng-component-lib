import { ClFormMessageDirective } from './form-message.directive';

describe('FormMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ClFormMessageDirective();
    expect(directive).toBeTruthy();
  });
});
