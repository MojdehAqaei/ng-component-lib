import { ClDragDropDirective } from './drag-drop.directive';

describe('DragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new ClDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
