import { ClBadgeDirective } from './badge.directive';

describe('BadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
